# Level 20: Shop

A simple state manipulation vulnerability! Just have to build a view function that returns substantially different values when it's called at different points in time (AKA different states).
